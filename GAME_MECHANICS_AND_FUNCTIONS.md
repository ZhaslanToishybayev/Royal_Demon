# 🎮 ROYAL DEMONS - Полное описание механик и функций

**Дата:** 2 ноября 2025
**Версия:** FINAL (система 4 сердец)
**Разработчик:** OpenJFX Team

---

## 📋 СОДЕРЖАНИЕ

1. [Основные механики](#1-основные-механики)
2. [Система здоровья](#2-система-здоровья)
3. [Система боя](#3-система-боя)
4. [Оружие и системы](#4-оружие-и-системы)
5. [Враги и ИИ](#5-враги-и-ии)
6. [Прогрессия и уровни](#6-прогрессия-и-уровни)
7. [Карты и навигация](#7-карты-и-навигация)
8. [Предметы и инвентарь](#8-предметы-и-инвентарь)
9. [UI и HUD](#9-ui-и-hud)
10. [Архитектура кода](#10-архитектура-кода)
11. [Системы и сервисы](#11-системы-и-сервисы)
12. [Проверенные баги](#12-проверенные-баги)

---

## 1. ОСНОВНЫЕ МЕХАНИКИ

### 1.1 Движение игрока
**Файлы:** `PlayerComponent.java:98-166`

```java
// Скорость движения: 180 единиц (баланс)
// Управление: WASD / стрелки
// Физика: реалистичная инерция
private double speed = 180;
```

**Особенности:**
- Плавное ускорение и замедление
- Нормализация скорости по осям X и Y
- Нет прыжков (2D платформер с фиксированной высотой)

### 1.2 Система анимации
**Компонент:** `WeaponAnimationComponent.java`, `ProjectileAnimationComponent.java`

```java
// Анимации игрока:
- idle: 0-3 кадры (0.5 сек)
- walk: 4-7 кадры (0.5 сек)
- attack: зависит от оружия

// Автоматическое переключение
if (physics.isMoving()) {
    texture.loopAnimationChannel(animWalk);
} else {
    texture.loopAnimationChannel(animIdle);
}
```

### 1.3 Система атаки
**Процесс атаки:**
1. **prepAttack** - подготовка (зажим ЛКМ)
2. **startAttack** - выполнение атаки
3. **ultimate** - специальная атака (Пробел)

---

## 2. СИСТЕМА ЗДОРОВЬЯ

### 2.1 Текущая реализация (FINAL)
```java
// HP игрока: 20 (4 сердца)
// 1 сердце = 5 HP
private void createHealthUI() {
    for (int i = 0; i < maxHealthPoints / 5; i++) {
        // 4 сердца при 20 HP
    }
}
```

### 2.2 Получение урона
**Файл:** `CreatureComponent.java:43-73`

```java
public void deductHealth(double point, double attackPower,
                         double blockProb, double armor, int pierce) {
    // Броня не может быть 0
    if (armor <= 0) armor = 1;
    
    // Проверка блока
    int blockRand = MainApp.getRandom().nextInt(100) + 1;
    if (blockRand <= blockProb * pierce) {
        return; // Атака заблокирована
    }
    
    // Урон с учетом брони
    int finalDamage = (int) Math.ceil(point * attackPower / armor);
    healthPoints -= finalDamage;
}
```

### 2.3 Система неуязвимости
**Файл:** `CreatureComponent.java:88-96`

```java
// После получения урона
isInvulnerable = true;
com.almasb.fxgl.dsl.FXGL.getGameTimer().runOnceAfter(() -> {
    isInvulnerable = false;
}, Duration.millis(!(this instanceof EnemyComponent) ? 500 : 100));
```

- **Игрок:** 500мс неуязвимости
- **Враги:** 100мс неуязвимости

### 2.4 Смерть
**Файл:** `GameOverWhenDie.java`, `DieScreenMenu.java`

```java
// При healthPoints <= 0
public void die() {
    healthPoints = 0;
    getDieBehavior().perform(); // Показывает экран смерти
}
```

---

## 3. СИСТЕМА БОЯ

### 3.1 Типы атак

#### А) Ближний бой
**Оружие:** Мечи (Golden Sword, Heavy Sword)
```java
public void attack() {
    // Создает хитбокс урона в направлении взгляда
    Entity hitbox = spawn("attackHitBox", playerX, playerY);
    hitbox.addComponent(new DamageComponent(attackPower));
}
```

#### Б) Дальний бой
**Оружие:** Лук (Bow)
```java
public void attack() {
    // Создает летящую стрелу
    Entity arrow = spawn("arrow", playerX, playerY);
    arrow.setDirection(aimDirection);
}
```

#### В) Магические атаки
**Оружие:** Посох (Magic Staff)
```java
public void attack() {
    // Создает магический снаряд
    Entity projectile = spawn("magicProjectile", playerX, playerY);
    projectile.addComponent(new ProjectileComponent());
}
```

### 3.2 Ultimate атаки

#### Golden Sword Ultimate
```java
public void ultimate() {
    // 10 кинжалов по кругу (360°)
    for (int i = 0; i < 10; i++) {
        double angle = i * 36; // 360/10 = 36°
        Entity dagger = spawnProjectile(angle);
    }
}
```

### 3.3 Система коллизий
**Количество обработчиков:** 14

```java
// Основные обработчики:
- PlayerEnemyCollisionHandler // Игрок ↔ Враг
- PlayerAttackEnemyCollisionHandler // Атака игрока ↔ Враг
- EnemyAttackPlayerCollisionHandler // Атака врага ↔ Игрок
- PlayerCoinCollisionHandler // Игрок ↔ Монета
- PlayerChestCollisionHandler // Игрок ↔ Сундук
// ...и 9 других
```

---

## 4. ОРУЖИЕ И СИСТЕМЫ

### 4.1 Типы оружия

#### Мечи
| Оружие | Урон | Скорость | Ultimate | Особенности |
|--------|------|----------|----------|-------------|
| Golden Sword 0 | 1.0 | Нормальная | Веер кинжалов | Начальное оружие |
| Golden Sword 1 | 1.5 | +10% | Улучшенный веер | Средний уровень |
| Golden Sword 2 | 2.0 | +20% | Максимальный веер | Высокий уровень |
| Heavy Sword | 2.5 | -30% | Мощный удар | Медленное, сильное |

#### Луки
| Оружие | Урон | Скорость | Ultimate | Особенности |
|--------|------|----------|----------|-------------|
| Bow 0 | 0.8 | Быстрая | Скорострел | Дешево, быстро |
| Bow 1 | 1.2 | Быстрая | 3 стрелы | Лучше баланс |
| Bow 2 | 1.5 | Очень быстрая | 5 стрел | Максимальный |

#### Магическое оружие
| Оружие | Урон | Скорость | Ultimate | Особенности |
|--------|------|----------|----------|-------------|
| Magic Staff 0 | 1.0 | Средняя | Магический взрыв | Базовый магический |
| Magic Staff 1 | 1.3 | Средняя | Взрыв + замедление | CC эффект |
| Magic Staff 2 | 1.6 | Быстрая | Множественные взрывы | Максимальный магический |

### 4.2 Система оружия
**Фабрика:** `WeaponFactory.java`

```java
// Создание оружия по ID
public static Weapon getWeaponById(String id) {
    return weaponMap.get(id);
}

// Регистрация оружия
private static void registerWeapons() {
    weapons.put("golden_sword_0", new GoldenSword0());
    weapons.put("bow_0", new Bow0());
    // ...и т.д.
}
```

### 4.3 Система анимации оружия
**Компонент:** `WeaponAnimationComponent.java`

```java
public WeaponAnimationComponent(String weaponType, double duration, int fpr) {
    // fpr = frames per row (кадры в ряду)
    // duration = длительность анимации
}
```

---

## 5. ВРАГИ И ИИ

### 5.1 Типы врагов

#### Обычные враги
```java
// Скорость: 25 пикселей/сек
// HP: 5-15 (в зависимости от типа)
// Урон: 1-3
```

#### Элитные враги
```java
// Скорость: 30 пикселей/сек
// HP: 20-30
// Урон: 3-5
// Особенность: больше опыта и золота
```

#### Мини-боссы
```java
// Скорость: 35 пикселей/сек
// HP: 40-60
// Урон: 5-8
// Особенность: уникальные способности
```

#### Финальный босс
```java
// Скорость: 40 пикселей/сек
// HP: 100
// Урон: 10
// Особенность: 2-3 специальные атаки
```

### 5.2 Система ИИ
**Фабрики:** `SimpleAI.java`, `AIFactory.java`

#### Типы ИИ:
1. **Простой ИИ** - идет прямо на игрока
2. **Адаптивный ИИ** - учитывает дистанцию и препятствия
3. **Тактический ИИ** - использует укрытия и тактику

```java
// Пример адаптивного ИИ
public class AdaptiveAI {
    private Vector2D target;
    private double detectionRange = 100;
    
    public void update() {
        double distance = calculateDistanceToPlayer();
        if (distance < detectionRange) {
            moveTowardsPlayer();
        } else {
            patrol();
        }
    }
}
```

### 5.3 Магические атаки врагов
**Файл:** `EnemyComponent.java:603-620`

```java
// При атаке fireball
put("weapon", !type.equals("finalboss")
    ? "fireball_enemy_64x64"  // Исправлено с fireball_enemy
    : "fireball_boss_64x64");

// Ultimate атака: веер огня
for (int i = 0; i < 5; i++) {
    spawnFireball(angle + i * 18);
}
```

---

## 6. ПРОГРЕССИЯ И УРОВНИ

### 6.1 Система уровней
**Файл:** `SimpleProgression.java`

```java
// Опыт за уровень
private int expForNextLevel(int level) {
    return level * 100; // Линейная прогрессия
}

// Лечение при апгрупде
private void applyLevelUpBonus() {
    Entity player = FXGL.geto("player");
    if (player != null) {
        PlayerComponent playerComp = player.getComponent(PlayerComponent.class);
        playerComp.increaseHealth(10); // 10 HP = 2 сердца
    }
}
```

### 6.2 Система достижений
**Файл:** `SimpleAchievements.java`

#### Достижения:
1. **Первая кровь** - 1 убийство (+50 золота)
2. **Собиратель золота** - 100 монет (+50 золота)
3. **Исследователь** - 10 комнат (+75 золота)
4. **Убийца драконов** - 50 убийств (+100 золота)
5. **Охотник за сокровищами** - 20 сундуков (+100 золота)
6. **Мастер уровней** - 10 уровень (+150 золота)
7. **Мастер комбо** - 25 комбо (+125 золота)

```java
public void checkAchievements() {
    // Проверка и разблокировка достижений
    if (getKillsCount() >= 1 && !isUnlocked(FIRST_KILL)) {
        unlockAchievement(FIRST_KILL, "Первая кровь!", 50);
    }
}
```

### 6.3 Комбо система
**Компонент:** `ComboManager.java`

```java
// Комбо увеличивает урон
private void updateCombo() {
    if (timeSinceLastHit < COMBO_WINDOW) {
        comboCount++;
        damageMultiplier = 1.0 + (comboCount * 0.1); // +10% за каждое комбо
    } else {
        comboCount = 0;
        damageMultiplier = 1.0;
    }
}
```

---

## 7. КАРТЫ И НАВИГАЦИЯ

### 7.1 Генерация карты
**Файл:** `GameMap.java`

```java
// Процедурная генерация 10 комнат
public GameMap(int roomCount) {
    this.roomCount = roomCount;
    generateRooms();
    connectRooms();
}
```

#### Типы комнат:
- **Обычные** - базовые враги
- **Босс** - мини-босс
- **Челлендж** - особые испытания
- **Финальная** - финальный босс
- **Оружейная** - выбор оружия

### 7.2 Переходы между комнатами
**Файл:** `PlayerDoorCollisionHandler.java`

```java
public void onCollisionBegin(Entity player, Entity door) {
    Room newRoom = getRoomInDirection(door.getString("direction"));
    if (newRoom != null && newRoom.visited()) {
        loadRoom(newRoom);
    } else if (!curRoom.enemiesCleared()) {
        showMessage("Clear enemies before exploring!");
    }
}
```

---

## 8. ПРЕДМЕТЫ И ИНВЕНТАРЬ

### 8.1 Монеты
**Компонент:** `CoinComponent.java`

```java
public CoinComponent(int value) {
    this.value = value; // Обычно 1-10
    // Анимация: 4 кадра, 0.5 сек
    animIdle = new AnimationChannel(FXGL.image("coin_16x16.png"), 4,
            16, 16, Duration.seconds(0.5), 0, 3);
}
```

### 8.2 Зелья
**Файл:** `UI.java`

#### Зелье здоровья
```java
public static void useHealthPot() {
    component.increaseHealth(5); // 5 HP
    healthPotProperty.set(healthPotProperty.get() - 1);
}
```

#### Зелье ярости
```java
public static void useRagePot() {
    PlayerComponent.setIsAttackPowerBuffed(true);
    ragePotProperty.set(ragePotProperty.get() - 1);
}
```

### 8.3 Сундуки
**Файл:** `PlayerChestCollisionHandler.java`

```java
public void onCollisionBegin(Entity player, Entity chest) {
    // Случайная награда
    int reward = MainApp.getRandom().nextInt(3);
    switch (reward) {
        case 0: PlayerComponent.addGold(20); break;
        case 1: UI.addHealthPot(); break;
        case 2: UI.addRagePot(); break;
    }
    chest.removeFromWorld();
}
```

### 8.4 Артефакты
**Компонент:** `ArtifactComponent.java`

#### Варианты артефактов:
```java
case "ring_of_power": damageMultiplier *= 1.2; break;
case "amulet_of_health": increaseHealth(10); break; // Исправлено: было 50
case "boots_of_speed": speedMultiplier *= 1.3; break;
case "charm_of_crit": critChanceBonus += 0.15; break;
case "necklace_of_life": lifeSteal += 0.05; break;
```

**Статус:** НЕ ИСПОЛЬЗУЮТСЯ (согласно CHANGELOG_FINAL.md)

---

## 9. UI И HUD

### 9.1 Система HUD
**Файл:** `GameHUD.java`

#### Компоненты HUD:
- **Здоровье** - 4 сердца (20 HP)
- **Золото** - счетчик монет
- **Уровень** - текущий уровень игрока
- **Зелья** - количество зелий в инвентаре

```java
private void createHealthUI() {
    // Создаем 4 сердца (20 HP / 5)
    for (int i = 0; i < playerComponent.getMaxHealthPoints() / 5; i++) {
        Texture heart = FXGL.texture("items/ui_heart_full_32x32.png");
        healthBar.getChildren().add(heart);
    }
}
```

### 9.2 Локализация
**Менеджер:** `LocalizationManager.java`, `EnhancedLocalizationManager.java`

#### Поддерживаемые языки:
- **Русский** (основной)
- **Английский**

```java
// Пример переводов
ruTranslations.put("game.ui.gold", "Золото");
ruTranslations.put("game.ui.level", "Уровень");
ruTranslations.put("menu.main_menu", "Главное меню");
```

### 9.3 Меню
**Файлы:** `MainMenu.java`, `DieScreenMenu.java`

#### Функции меню:
- Начать игру
- Настройки
- Главное меню
- Выход
- Экран смерти (восстановление)

---

## 10. АРХИТЕКТУРА КОДА

### 10.1 Модульная система
**Менеджер:** `ModuleManager.java`

#### Модули:
- **CoreModule** - основные функции
- **AssetModule** - загрузка ресурсов

```java
public class ModuleManager {
    private List<Module> modules = new ArrayList<>();
    
    public void registerModule(Module module) {
        modules.add(module);
        module.init();
    }
}
```

### 10.2 Система компонентов

#### Основные компоненты:
```java
// Игрок
PlayerComponent extends CreatureComponent
- Управление
- Анимации
- Бой

// Враги
EnemyComponent extends CreatureComponent
- ИИ
- Атаки

// Физика
PhysicsComponent
- Движение
- Коллизии
```

### 10.3 Фабрики

#### Список фабрик:
- `CreatureFactory` - создание игрока и врагов
- `WeaponFactory` - создание оружия
- `AIFactory` - создание ИИ
- `ProjectileFactory` - создание снарядов
- `AnimationFactory` - создание анимаций

---

## 11. СИСТЕМЫ И СЕРВИСЫ

### 11.1 Логирование
**Файл:** `GameLogger.java`

#### Уровни логирования:
- **DEBUG** - отладочная информация
- **INFO** - общая информация
- **GAMEPLAY** - игровые события
- **WARNING** - предупреждения
- **ERROR** - ошибки
- **SYSTEM** - системные сообщения

```java
GameLogger.debug("Loading assets");
GameLogger.info("Player spawned");
GameLogger.warning("Missing texture: sword.png");
GameLogger.error("Failed to load map", e);
```

### 11.2 Конфигурация
**Файл:** `ConfigManager.java`

#### Настройки:
- Настройки игры
- Управление
- Графика
- Звук
- Автобэкап конфигураций

```java
public class ConfigManager {
    private Config config;
    
    public void loadConfig(String path) {
        config = parseConfig(path);
    }
    
    public void saveConfig() {
        backupConfig();
        writeConfig(config);
    }
}
```

### 11.3 Ресурс-менеджмент
**Файл:** `ResourceManager.java`

```java
public class ResourceManager {
    // Загрузка из JAR
    public InputStream loadResourceFromJar(String path);
    
    // Загрузка из файловой системы
    public InputStream loadResourceFromFile(String path);
    
    // Кэширование
    private Map<String, Resource> cache;
}
```

### 11.4 Сохранение игры
**Файлы:** `GameSaveService.java`, `GameSaveData.java`

```java
public class GameSaveData implements Serializable {
    private int level;
    private int gold;
    private int healthPoints;
    private List<String> unlockedWeapons;
}
```

---

## 12. ПРОВЕРЕННЫЕ БАГИ

### 12.1 Исправленные баги (FINAL)

#### 1. Фиолетовые артефакты
**Файлы:** UI.java, ComboFactory.java, AIFactory.java
**Проблема:** PURPLE/MAGENTA цвета при стрельбе из Magic Staff
**Решение:** Заменены на ORANGE/RED/CYAN

#### 2. Ошибка загрузки magic1_32x32.png
**Файл:** ProjectileAnimationComponent.java
**Проблема:** Неправильный путь `./weapons/`
**Решение:** Изменен на `weapons/`

#### 3. Ошибка загрузки fireball_enemy.png
**Файл:** EnemyComponent.java
**Проблема:** Неполные имена спрайтов
**Решение:** `fireball_enemy` → `fireball_enemy_64x64`

#### 4. HP игрока = 100
**Файл:** CreatureFactory.java
**Проблема:** Не соответствует финальной версии
**Решение:** 100 → 20 HP (4 сердца)

#### 5. Отображение сердечек
**Файл:** GameHUD.java
**Проблема:** 1 сердце = 10 HP, при 5 HP показывало 0
**Решение:** 1 сердце = 5 HP + setVisible(false)

#### 6. Округление урона
**Файл:** CreatureComponent.java
**Проблема:** Дробные значения урона
**Решение:** `Math.ceil(damageDealt)`

#### 7. Долгая неуязвимость
**Файл:** CreatureComponent.java
**Проблема:** 2000мс при HP = 5
**Решение:** 2000мс → 500мс

#### 8. Конфликт HUD
**Файлы:** BossComponent.java, PlayerDoorCollisionHandler.java
**Проблема:** UI.init() перезаписывал GameHUD
**Решение:** Убраны вызовы UI.init()

### 12.2 Синхронизация монет
**Файл:** PlayerComponent.java
**Исправление:** `addGold()` синхронизирует FXGL и UI

---

## 📊 СТАТИСТИКА ПРОЕКТА

### Код:
- **Java файлов:** 153
- **Строк кода:** ~15,000
- **Пакетов:** 20+

### Игровые системы:
- **Компонентов:** 30+
- **Фабрик:** 10+
- **Обработчиков коллизий:** 14
- **Типов оружия:** 8
- **До достижений:** 7

### Ресурсы:
- **Текстуры:** 100+
- **Звуки:** 50+
- **Музыка:** 10+
- **Локализации:** 2 (RU/EN)

---

## 🎯 ЗАКЛЮЧЕНИЕ

**Royal Demons** - это полноценная 2D RPG с:

✅ **Сбалансированным боем** - 4 типа оружия  
✅ **Процедурными картами** - 10 комнат  
✅ **Прогрессией** - уровни, достижения, комбо  
✅ **Ресурсами** - монеты, зелья, сундуки  
✅ **Архитектурой** - компоненты, фабрики, модули  
✅ **Качеством кода** - логирование, конфиг, тесты  

**Игра готова к игре и распространению!**

---

## 👨‍💻 АВТОРЫ

**Разработчики:** OpenJFX Team
**Год разработки:** 2024-2025
**Лицензия:** Academic Free License 3.0

---

**🎮 Royal Demons - Legend of the Lost Kingdom 🎮**
