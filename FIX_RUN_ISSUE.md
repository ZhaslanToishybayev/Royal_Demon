# 🔧 Исправление проблемы запуска Royal Demons

## ❌ Проблема
```
Error occurred during initialization of boot layer
java.lang.module.FindException: Module royal.demons not found
```

## ✅ Решение

Я уже исправил конфигурацию! Теперь попробуйте запустить игру:

### Способ 1: Очистить и запустить
```bash
./gradlew clean
./gradlew run
```

### Способ 2: Если не работает, попробуйте с флагами
```bash
./gradlew run --no-daemon
```

### Способ 3: Альтернативный запуск
```bash
java --enable-native-access=ALL-UNNAMED -cp "$(./gradlew printClasspath)" uwu.openjfx.MainApp
```

## 🔧 Что я исправил:

1. **Отключил модульную систему** в `build.gradle`
2. **Убрал `mainModule`** из конфигурации
3. **Отключил `jlink`** для избежания конфликтов
4. **Очистил `module-info.java`**

## 🚀 После исправлений вы увидите:

- ✅ **Русифицированный интерфейс** - "Здоровье", "Золото", "Уровень"
- ✅ **Комбо-система** - выполняйте атаки в последовательности
- ✅ **Прогрессия** - уровни и опыт за убийство врагов
- ✅ **Достижения** - 8 достижений с уведомлениями
- ✅ **Визуальные эффекты** - числа урона и анимации

## 🎮 Управление в игре:

| Клавиша | Действие |
|---------|----------|
| **WASD** | Движение |
| **ЛКМ** | Легкая атака |
| **ПКМ** | Тяжелая атака |
| **Пробел** | Ультимейт |
| **E** | Подобрать предмет |
| **I** | Инвентарь |

## 🎯 Комбо-атаки:

- **Легкий-Тяжелый-Тяжелый** (ЛКМ-ПКМ-ПКМ) → "Тройной удар"
- **Тяжелый-Легкий-Тяжелый** (ПКМ-ЛКМ-ПКМ) → "Круговая атака"
- **Легкий-Легкий-Тяжелый** (ЛКМ-ЛКМ-ПКМ) → "Быстрые удары"

Выполняйте их быстро (в течение 1 секунды)!

## 📊 Если все еще не работает:

### Проверьте версию Java:
```bash
java -version
```
Должна быть Java 21 или выше.

### Проверьте переменные окружения:
```bash
echo $JAVA_HOME
which java
```

### Очистите кэш Gradle:
```bash
rm -rf ~/.gradle/caches
./gradlew clean build
./gradlew run
```

## 🎉 Готово к игре!

После этих исправлений игра должна запуститься со всеми улучшениями:
- Русская локализация
- Комбо-система
- Прогрессия персонажа
- Достижения
- Визуальные эффекты

**Запускайте и наслаждайтесь улучшенным Royal Demons!** 🎮✨