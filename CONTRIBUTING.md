# 🤝 Руководство по участию в проекте Royal Demons

Спасибо за интерес к проекту Royal Demons! Это руководство поможет вам внести свой вклад в развитие игры.

---

## 📋 Содержание

1. [Как внести вклад](#как-внести-вклад)
2. [Процесс разработки](#процесс-разработки)
3. [Стандарты кода](#стандарты-кода)
4. [Сообщения об ошибках](#сообщения-об-ошибках)
5. [Предложение фич](#предложение-фич)
6. [Приоритеты](#приоритеты)

---

## 🎯 Как внести вклад

### Для новичков

1. **Форкните** репозиторий
2. **Клонируйте** свой форк:
   ```bash
   git clone https://github.com/YOUR_USERNAME/Royal_Demons.git
   cd Royal_Demons
   ```
3. **Создайте ветку** для фичи:
   ```bash
   git checkout -b feature/amazing-feature
   ```
4. **Внесите изменения**
5. **Протестируйте** (см. ниже)
6. **Создайте Pull Request**

---

## 🔄 Процесс разработки

### 1. Подготовка среды

**Требования:**
- Java 25+
- Gradle 8.7+
- Git

**Клонирование:**
```bash
git clone <repository-url>
cd Royal_Demons
```

**Сборка проекта:**
```bash
./gradlew clean build
```

**Запуск игры:**
```bash
./gradlew run
```

### 2. Создание ветки

Используйте префиксы веток:
- `feature/` - новая фича
- `fix/` - исправление бага
- `refactor/` - рефакторинг кода
- `docs/` - обновление документации
- `test/` - добавление тестов

Примеры:
```bash
git checkout -b feature/new-weapon-system
git checkout -b fix/null-pointer-exception
git checkout -b refactor/player-component
```

### 3. Тестирование изменений

**Обязательно проверьте:**

#### Unit тесты
```bash
./gradlew test
```

#### Код стайл
```bash
./gradlew checkstyleMain
./gradlew spotbugsMain
```

#### Сборка без тестов
```bash
./gradlew build -x test
```

#### Ручное тестирование
1. Запустите игру: `./gradlew run`
2. Проверьте основной геймплей:
   - Движение (WASD)
   - Атаки (ЛКМ/ПКМ)
   - Сбор предметов
   - Бой с врагами
   - Сохранения

### 4. Commit сообщения

Используйте формат:
```
тип( область): описание

Описание изменений (опционально)

Breaking changes (опционально)
```

**Типы:**
- `feat` - новая фича
- `fix` - исправление бага
- `docs` - документация
- `style` - форматирование кода
- `refactor` - рефакторинг
- `test` - тесты
- `chore` - сборка/зависимости

**Примеры:**
```
feat(player): add weapon switching functionality
fix(collision): resolve null pointer in PlayerEnemyCollisionHandler
docs(readme): update installation instructions
```

---

## 📝 Стандарты кода

### Java Code Style

**Отступы:**
- Используйте 4 пробела (не табы)
- IDE должна настроить это автоматически

**Именование:**
- **Классы:** PascalCase (`PlayerComponent`)
- **Методы/переменные:** camelCase (`getHealthPoints()`)
- **Константы:** UPPER_SNAKE_CASE (`MAX_HEALTH`)
- **Пакеты:** lowercase (`uwu.openjfx.components`)

**Организация кода:**

```java
package uwu.openjfx.components;

import java.util.List;

/**
 * Краткое описание класса.
 *
 * @author Ваше имя
 */
public class ExampleComponent {

    // Константы
    private static final int MAX_HEALTH = 100;

    // Поля
    private int health;

    // Конструкторы
    public ExampleComponent(int health) {
        this.health = health;
    }

    // Геттеры/сеттеры
    public int getHealth() {
        return health;
    }

    public void setHealth(int health) {
        this.health = health;
    }

    // Методы
    public void heal(int amount) {
        health = Math.min(health + amount, MAX_HEALTH);
    }

    // Private методы
    private void validateHealth(int health) {
        if (health < 0) {
            throw new IllegalArgumentException("Health cannot be negative");
        }
    }
}
```

### Лучшие практики

✅ **Делайте:**
- Пишите JavaDoc для публичных методов
- Добавляйте проверки на null
- Используйте логирование (`GameLogger`)
- Создавайте тесты для новой функциональности
- Следуйте принципам SOLID
- Разделяйте ответственность

❌ **Не делайте:**
- Не используйте `System.out.println` (используйте `GameLogger`)
- Не оставляйте закомментированный код
- Не используйте `static` без необходимости
- Не захардкожьте магические числа
- Не игнорируйте Checkstyle/SpotBugs
- Не создавайте гигантские методы (>100 строк)

### Работа с Git

**Не коммитьте:**
- Build файлы (в `.gitignore`)
- Логи (`.log` файлы)
- IDE настройки (`.idea/`, `.vscode/`)
- Временные файлы (`*.tmp`)

**Коммитьте только:**
- Исходный код (`.java`)
- Ресурсы (`.png`, `.wav`, `.tmx`)
- Конфигурацию сборки (`build.gradle`)
- Документацию (`.md`)

---

## 🐛 Сообщения об ошибках

### Перед созданием Issue

1. **Проверьте** существующие issues
2. **Обновите** до последней версии кода
3. **Проверьте** [отчеты о проблемах](DETAILED_PROBLEM_ANALYSIS.md)

### Шаблон для бага

```markdown
## Описание бага

Краткое описание проблемы.

## Воспроизведение

Шаги для воспроизведения:
1. Запустите игру
2. Нажмите на ...
3. Прокрутите до ...
4. Увидите ошибку

## Ожидаемое поведение

Что должно было произойти.

## Фактическое поведение

Что произошло на самом деле.

## Скриншоты

Если применимо, добавьте скриншоты.

## Окружение

- OS: [Ubuntu/Windows/Mac]
- Java: [версия]
- Версия игры: [версия]

## Дополнительная информация

Любая другая информация.
```

---

## 💡 Предложение фич

Мы приветствуем новые идеи! Перед созданием Feature Request:

### Проверьте:
- Фит ли это в архитектуру проекта?
- Есть ли подобное в [планах развития](README.md#план-развития)?
- Улучшит ли это игровой опыт?

### Шаблон для фичи

```markdown
## Предлагаемая функция

Краткое описание.

## Проблема

Какую проблему решает эта фича.

## Предлагаемое решение

Как вы видите решение.

## Альтернативы

Какие альтернативы вы рассматривали?

## Дополнительная информация

Скриншоты, наброски, ссылки на ресурсы.
```

---

## 🎯 Приоритеты

### Сейчас (Критические)

🔴 **P0 - Срочно:**
- Исправление крашей
- Утечки памяти
- Проблемы со сборкой
- Потеря данных (сохранения)

🟠 **P1 - Высокий:**
- Производительность
- Баланс игры
- Критические баги

### Потом (Средние и низкие)

🟡 **P2 - Средний:**
- Новые фичи
- Рефакторинг
- Документация

🟢 **P3 - Низкий:**
- Улучшения UX
- Визуальные эффекты
- Необязательные функции

---

## 📞 Получение помощи

- **Issues:** [GitHub Issues](issues)
- **Обсуждения:** [GitHub Discussions](discussions)
- **Документация:** [README.md](README.md)

---

## 📜 Кодекс поведения

Мы придерживаемся принципов открытости и уважения. Пожалуйста:

- Будьте вежливы и конструктивны
- Уважайте мнения других
- Фокусируйтесь на деле, а не на личностях
- Демонстрируйте эмпатию к другим участникам

Нарушение кодекса поведения может привести к исключению из проекта.

---

## 🎉 Спасибо!

Спасибо за ваш вклад в Royal Demons! Каждый PR, баг-репорт и идея помогают сделать игру лучше.

**Ваше участие ценится!** 🚀

---

*Этот документ является живым. Предлагайте улучшения через PR!*
